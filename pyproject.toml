[project]
name = "symcogitant"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Siarhei Psiola", email = "s.psiola@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "anthropic>=1.7.0",
    "fastapi>=0.141.1",
    "google-genai>=2.24.0",
    "httpx>=0.28.1",
    "openai>=3.16.2",
    "psutil>=7.2.2",
    "python-dotenv>=1.2.3",
    "pyyaml>=6.0.3",
    "telethon>=1.45.0",
    "uvicorn>=0.53.0",
    "websockets>=13.0",
]

[project.scripts]
symcogitant = "symcogitant:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.pyright]
extraPaths = ["."]
venvPath = "."
venv = ".venv"
reportMissingModuleSource = false

[dependency-groups]
dev = [
    "types-pyyaml>=6.0.12.20260906",
]
